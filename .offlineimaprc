[general]
pythonfile = ~/.offlineimap.py
accounts   = Gmail

[Account Gmail]
localrepository  = Local
remoterepository = Remote
status_backend   = sqlite

[Repository Local]
localfolders = ~/Gmail
type         = Maildir

[Repository Remote]
sslcacertfile    = ~/.mutt/certificates
folderfilter     = lambda foldername: foldername not in ['[Gmail]/Trash', '[Gmail]/All Mail']
maxconnections   = 5
remoteuser       = sgianazza@gmail.com
remotepasseval   = get_imap_password()
type             = Gmail
readonly         = True
